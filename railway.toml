# Railway: ビルド・起動・ヘルスチェックを明示（空白画面・接続失敗の切り分けをしやすくする）
# https://docs.railway.com/reference/config-as-code

[build]
builder = "nixpacks"
buildCommand = "npm run build"

[deploy]
startCommand = "node server.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
