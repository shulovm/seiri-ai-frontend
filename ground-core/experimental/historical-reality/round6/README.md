# Round 6 — territorial composition calibration

Independent additive envelope; Round1–5/core remain immutable. Four bounded cases: Seoul protocol/telegraph request, Niuchwang consular/native-customs functions, Uiju withdrawal/property/communications, and CER district infrastructure-protection duty. The CER case has network-level resolution, not an authenticated railway segment.

`territory.ts` composes the unchanged Round3 documentary and Round5 source-assertion contracts. Open source-derived dimensions, functions, populations, infrastructure, temporal keys and assertion forms remain experimental. It offers exact scoped reported-proposition queries, conservative contrast comparison and Source traversal. No generic ontology or canonical territorial controller is introduced.

The opt-in `territorial-attributed-mentions/v1` enrichment retains scoped reconstruction context inside existing core Claim JSON values. Claims target source documents. This proves storage/roundtrip and attribution, **not** native core semantics for authority or external Reality. Historical time never becomes ingestion applicability. The sidecar remains necessary for territorial queries, uncertainty and reconstruction.

Replay from repository root:

```sh
node --import tsx ground-core/experimental/historical-reality/round6/replay.ts
python3 ground-core/experimental/historical-reality/round6/validate-provenance.py
node --import tsx --test ground-core/__tests__/historical-reality-round6.test.ts
node_modules/.bin/tsc -p ground-core/tsconfig.json --pretty false
```

Replay checks 703 legacy SHA pins, all inherited/new assets and materialize/file-store roundtrip. Use another output directory to re-run earlier rounds; do not overwrite their pinned replay artifacts. Full core tests require the normal local IPC capability used by the inherited CLI tests.

Published editions/transcriptions and source-heading dates are distinct from originals and manifestation creation/publication. NIKH original images were not inspected; its saved extracts are current researcher-selected notes. FRUS elements/text are derivatives of the saved official TEI, checked by `validate-provenance.py`. Source-content clarity does not authenticate original artifacts or establish enforcement. CER protection does not imply operation/ownership. No authenticated exclusive same-dimension territorial conflict was located; the competing-position test is explicitly synthetic.

See `composition-change-record.json`, `semantic-gap-signals.json`, the 18 B revalidations in the dataset, and the Round6 report for boundaries and remaining evidence needs.
