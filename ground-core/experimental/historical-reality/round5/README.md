# Round 5 — semantic consolidation gate

No new historical search, no new core primitive, canonical relation, collection or schema change. Round1–4 are immutable (`immutable-baseline.json`). The review index normalizes SG identifiers only for lookup; original identifiers and records are retained.

`candidate-inventory.json` covers every SG01–35 and the user inventory: 33 semantic candidates classified A–E. **Opaque JSON preservation is not counted as a generic core semantic contract.** A covers existing atoms with correct intended use; B records missing role/target/upstream/coverage/derivation composition constraints; D retains domain vocabulary; E refuses premature lifecycle/identity/independence/calibration. C is empty.

The prior Round4 recommendations are not binding promotions. Re-audit found Claim/Evidence, structural EpistemicGap, declaration-holder separation, exact unit semantics, derived BeliefAssessment and append-only decision snapshots already solve more than the initial partial source adapter exposed. Current GROUND resource-availability fixtures provide a concrete nonhistorical SOURCE ASSERTION ≠ objective truth analogue. They do not demonstrate probabilistic independence, arbitrary reconstruction supersession, or warranted Knowledge.

Before implementation `promotion-records.json` records the motivating historical cases, invariant, existing representation/failure, alternatives, proposed representation, migration impact/risk and tests. The only accepted change is an **opt-in experimental adapter usage contract** using existing0.1.24atoms. It preserves original claimant/chain/source scope/raw time inside source-content Claims. It is a partial projection, not lossless sidecar migration, nor a core semantic promotion.

`source-contract.ts` implements the adapter proof. `replay.ts` reloads all four datasets, checks675preexistingfilepins, tests candidate roots/signal coverage/domain fixture existence, traverses historical derived records, persists/reloads existing core source Assertions, and replays case proofs. Generic lineage, actor Knowledge, uncertainty model, time conversion and reconstruction remain in original sidecars.

Required results are in the Round5 report and `verification.json`; no approval or future follow-up is needed to inspect this completed gate.

Review discovered an additional adapter issue: broad predicate conflated separate text propositions. New source_assertion/<claimID> slots respect existing Belief query keys. The old adapter is preserved and replayed for comparison. No legacy core behavior is changed.
