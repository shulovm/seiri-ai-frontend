import{it}from'node:test';import assert from'node:assert/strict';import{compareSourceTimes,wholeTimeEdges,knowledgeTimeEligibility}from'../reality/whole-time.js';
it('reordered prose/timeline does not reverse explicit source clocks',()=>assert.deepEqual(wholeTimeEdges([{index:0,expression:'10:00'},{index:1,expression:'09:00'},{index:2,expression:'09:30'}]),[{from:1,to:2},{from:2,to:0}]));it('late information is excluded while earlier information is not automatically maker knowledge',()=>{assert.equal(knowledgeTimeEligibility('10:00','09:30'),'definitely later: exclude');assert.equal(knowledgeTimeEligibility('09:00','09:30'),'reported before: holder not established');});it('relative day order does not need a guessed calendar date',()=>{assert.equal(compareSourceTimes('前日16:00','当日09:00'),'before');assert.equal(compareSourceTimes('3日前','当日06:00'),'before');});it('mixed absolute/local bases and vague same-day ranges remain unresolved',()=>{assert.equal(compareSourceTimes('2027-01-01T09:00:00Z','09:30'),'unknown-or-overlapping');assert.equal(compareSourceTimes('1か月前','3週間前'),'unknown-or-overlapping');});

import{createEmptyProject,applyPatch}from'../state-engine.js';import{proposeFromReality}from'../reality/propose.js';
it('whole composition explicitly excludes later reports from a dated selected-plan record',()=>{const input='### Reality本文\n09:30にF案を正式に選んだ。\n### 分かっていないこと\n- 原因\n### 時系列\n- 10:00 検査結果を報告\n- 09:00 作業開始\n- 11:00 現在';const s=createEmptyProject({title:'late report'});const p=proposeFromReality(s,{project_id:s.project.id,input_text:input},'canonical');assert.ok('proposed_patch'in p.result);if('proposed_patch'in p.result){const next=applyPatch(s,p.result.proposed_patch);assert.ok(next.reality_states.some(x=>x.kind==='later_report_excluded'&&x.value===true));assert.equal(next.reality_decision_declarations.length,0);}});
